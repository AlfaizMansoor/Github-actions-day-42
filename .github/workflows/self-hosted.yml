name: self-hosted

on:
    push:
        branches: [main]

jobs:
    self:
        runs-on: [my-linux-runner]
        steps:
            - name: my runner
              run: |
                hostname
                pwd
                touch my-self-hosted-runner.txt
                [ -f my-self-hosted-runner.txt ] && echo "Exists"